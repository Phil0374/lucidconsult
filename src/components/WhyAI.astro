---
import { t, type Lang } from "../i18n";
const { lang = "en" as Lang } = Astro.props;
const keys = ["why1","why2","why3","why4"];
---

<section id="why-ai" class="relative py-20 md:py-28">
  <div class="mx-auto max-w-6xl px-6">
    <div class="reveal mb-14 max-w-2xl">
      <p class="text-xs font-semibold uppercase tracking-widest text-brand-400">{t(lang, "why_badge")}</p>
      <h2 class="mt-3 font-display text-3xl font-bold text-white md:text-5xl" set:html={t(lang, "why_title")} />
      <p class="mt-4 text-lg leading-relaxed text-ink-200">{t(lang, "why_subtitle")}</p>
    </div>

    <div class="grid gap-6 md:grid-cols-2">
      {keys.map((k) => (
        <div class="reveal glass rounded-2xl p-7">
          <div class="mb-4 flex h-10 w-10 items-center justify-center rounded-lg bg-brand-500/10 text-brand-400">
            <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="1.8"><path stroke-linecap="round" stroke-linejoin="round" d="M12 9v3.75m9-.75a9 9 0 11-18 0 9 9 0 0118 0Zm-9 3.75h.008v.008H12v-.008Z"/></svg>
          </div>
          <h3 class="font-display text-lg font-semibold text-white">{t(lang, `${k}_title`)}</h3>
          <p class="mt-2 text-sm leading-relaxed text-ink-200">{t(lang, `${k}_text`)}</p>
        </div>
      ))}
    </div>
  </div>
</section>
